# Pending Tweaks — fold into the NEXT checkpoint round

Uncommitted local changes staged for a later version bump/commit. Empty = nothing pending.

*(How this works: when the operator says "make the change but don't commit now," the tweak is logged
here with its files + rationale, then bundled into the next checkpoint and removed from this list.)*

---

## No pending tweaks.

- **TWEAK-001** (sharpen `NO STAGED EVIDENCE` clause) — **SHIPPED in v1.0.0.179** (2026-07-12), bundled with the critical `search_web` `NameError` hotfix. See `CHANGELOG_v1.0.0.179.md`.
