# Requirements for fully_functional_stocks_charting_software_1516
# This file lists the Python libraries required to run the application.
# It has been updated to support real-time data ingestion from Yahoo Finance 
# to replace the previous fake data generation logic.

# --- Core Data Handling ---
# pandas is required for data structure manipulation (DataFrames) used throughout the app.
pandas>=1.5.3

# numpy is used for numerical operations and array handling.
numpy>=1.24.2

# python-dateutil provides powerful extensions to the standard datetime module.
python-dateutil>=2.8.2

# --- Data Source (Yahoo Finance) ---
# yfinance is the primary library for fetching real-time stock data from Yahoo Finance.
# This is the critical dependency for fixing the data source.
yfinance>=0.2.28

# pytz is used for timezone localization (essential for accurate market open/close times).
pytz>=2023.3

# --- Visualization & UI Support ---
# plotly is likely used for rendering the interactive stock charts.
plotly>=5.14.1

# --- Networking & Utilities ---
# requests is used for HTTP requests, often a dependency for data fetching or API calls.
requests>=2.31.0

# --- Configuration ---
# python-dotenv allows loading environment variables from a .env file for config management.
python-dotenv>=1.0.0